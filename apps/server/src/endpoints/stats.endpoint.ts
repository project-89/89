/**
 * MIGRATED: Removed pure CRUD handlers
 * 
 * DELETED:
 * - handleGetStats → Use GET /api/model/userStats/:id
 * - handleUpdateStats → Use PATCH /api/model/userStats/:id
 * 
 * ALL endpoints in this file were CRUD - file can be deleted
 */
