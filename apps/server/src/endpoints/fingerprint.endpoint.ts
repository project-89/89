/**
 * MIGRATED: Removed pure CRUD handlers
 * 
 * DELETED:
 * - handleRegisterFingerprint → Use POST /api/model/fingerprint
 * - handleGetFingerprint → Use GET /api/model/fingerprint/:id
 * - handleUpdateFingerprint → Use PATCH /api/model/fingerprint/:id
 * 
 * ALL endpoints in this file were CRUD - file can be deleted
 */
