/**
 * MIGRATED: Removed pure CRUD handlers
 * 
 * DELETED:
 * - handleCreateImpression → Use POST /api/model/impression
 * - handleGetImpressions → Use GET /api/model/impression
 * - handleDeleteImpressions → Use DELETE /api/model/impression/:id
 * 
 * ALL endpoints in this file were CRUD - file can be deleted
 */
